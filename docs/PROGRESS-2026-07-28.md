# Progress / Timesheet — 2026-07-28

Multilingual **News & Events + News Ticker + Blog + Webinar** implementation across all
6 locales, plus UI-polish and contact/address corrections. All deployed to dev
(`wardjet.pixels2pixels.ch`). Much of the work is **data** (ACF fields, term-meta
translations, pages, ticker config, images) that lives in the dev DB, not in git.

## Work log

### News & Events + News Ticker  (~4.0h)
- Taxonomy: renamed **Open House → CNC Expo** (events); created **Promotions** (news).
  Media Coverage + Product Launches already existed.
- Configured the ticker (Settings → News Ticker) to the blueprint's categories:
  **CNC Expo + Trade Shows** (events), **Promotions** (news).
- Localized news/events category term labels (es/fr/pl): Trade Shows, Product Launches,
  Promotions, Media Coverage (from blueprint); CNC Expo kept English.
- Language switcher fix: relinked es/fr/pl news pages into the `news-events-2024` group
  and switched them from the broken `wardjet-news.php` (queried blog `post`s) to
  `newsandevents.php`.
- Localized single-article URLs: archive links carry the current-locale prefix
  (`temp_lang_code`); switcher aliases `news_and_events` to every locale.
- Ticker read-more button URL fix: `wj_get_news_events_url()` queried group
  `news-events-2025` (nonexistent) → fell back to wrong `/cc/ll/news-events/`; corrected
  to `news-events-2024` so it resolves each locale's real page.
- Archive de-duplication: localized archives show **own-locale articles + English
  fallback, each article once** (`wj_localized_archive_exclude()` + `post__not_in`).
- Ticker QA: created 12 `[TEST]` articles (1 per category × en/es/fr/pl); verified
  per-locale translation swap, date expiry, locale targeting, and English fallback when
  a translation is unpublished.

### Blog + Webinar multilingual  (~2.0h)
- Created 6 es/fr/pl Blog + Webinar archive pages on the FacetWP `newsandevents`-style
  templates (blog=1326x, webinar=1326x), parented to each locale's homepage.
- Localized blog taxonomy terms (technical_topics, blog-industry, blog-material);
  blog-product series kept English per request.
- Applied the same locale-prefix links + switcher aliasing + archive dedup as news.

### UI polish  (~1.5h)
- Feature-slider yellow divider → full-width (desktop + mobile = card width); es/fr/pl
  short-title cleanup + divider-height safety net.
- Product-category underlines → WardJet blue `#0D3159` on the homepage grid only;
  removed on products-page grid + flyout.
- KPI `10.000+` → `10,000+` (comma) across all locales.
- Selected-product spec labels (LENGTH/WIDTH) localized, Z-TRAVEL kept English;
  M-Series selected-products count → 12 across all locales.
- New machine images (M/J/Custom) applied to cards, 3D render, flyout, grid.

### Contact / addresses  (~1.0h)
- Contact-section cards (6 locales) → blueprint addresses + "AXYZ Automation Group (AAG)";
  USA cards dual phone `(800) 361-3408` + `(844) WARDJET`; multi-phone template rendering.
- Footer HQ Canada phone → `+1 (800) 361-3408` (all locale variants).
- Call-Us CTA (USA) → `(800) 361-3408`; Email-Us CTA → `info@wardjet.com` (all locales).
- Locations cards: "AXYZ AND WARDJET" → "WardJet AND AXYZ" (all locales, 18 cards).

**Total: ~8.5h**

## Code changed (this commit)
- `plugins/wj-multilingual/includes/menu.php` — switcher aliases news/blog/webinar to all locales
- `themes/wardjet/functions.php` — `wj_localized_archive_exclude()` dedup helper
- `themes/wardjet/newsandevents.php`, `blogs.php`, `page-webinars.php` — locale-prefix links + dedup
- `themes/wardjet/inc/ticker-functions.php` — news-events URL group fix
- `themes/wardjet/template-parts/agg-contact.php` — multi-phone rendering
- `themes/wardjet/template-parts/section-selected_products.php` — spec-label localization
- `themes/wardjet/template-parts/new-kpis.php` — KPI default 10,000+
- `themes/wardjet/template-parts/features-slider.php`, `inc/assets/js/wardjet-custom.js`,
  `inc/assets/css/parts/features-slider.css` — full-width divider
- `themes/wardjet/inc/assets/css/parts/products-section.css`, `megamenu-content.php` — category underlines
- `themes/wardjet/wardjet-industries.php` + remaining mobile-polish CSS
  (`feature-block.css`, `footer.css`, `header.css`, `wardjet-custom.css`)

## Data / server changes (dev DB — not in git)
Term-meta translations (news + blog taxonomies), `wj_ticker_options`, contact_localized
+ footer_headquarters ACF, 6 new archive pages, 12 `[TEST]` ticker articles, machine
images (attachments 13258–13260), M-Series `series_image`/selected_products updates.
